//import config
const config = require('../../config/config.json');

var TAG = "./src/data/ValidateRequest.js => ";

module.exports = class ContentModel {
    static ValidateRequest(result) {
        
    }
}